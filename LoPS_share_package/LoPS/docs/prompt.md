当前项目的整体目标是：对我此前完成的一些科研代码进行系统性重构。为此，我会授予你读取和执行其他文件夹中代码及相关文件的权限。

接下来，我会向你提供以下信息：

（1）目标脚本
我会指定本轮需要分析和重构的具体脚本（是该项目外其它已有项目中的脚本）。

（2）运行环境与数据来源
我会说明该脚本应在哪个环境中运行，以及脚本运行时所依赖的数据应如何读取。

你的任务包括：

（1）理解原始功能
阅读目标脚本及其依赖的相关文件，准确理解该脚本当前实现的功能、输入输出、执行流程以及与其他代码或数据之间的关系。

（2）评估模块化必要性
分析该功能的复杂程度、复用价值和边界清晰度，并据此判断是否有必要在 src/LoPS 下创建独立模块文件来承载该功能。

（3）制定重构方案
在充分理解原始代码的基础上，分析应如何进行彻底重构。重构方案应包括但不限于：功能拆分、文件拆分、模块边界划分、数据路径组织、接口设计以及验证方式等内容。

（4）等待确认后执行重构
在你提出的重构方案得到我的同意之前，不要直接修改代码。只有在方案确认后，才开始执行具体的重构工作。

（5）整理数据与运行脚本
重构完成后，可以根据需要将原代码中使用的数据移动到 data 目录下的合适位置。同时，应在 script 目录中编写用于运行该模型或功能的脚本，并使用整理后的数据执行该脚本，以检查运行结果。

（6）验证重构一致性
重构后的代码必须通过一致性验证：在使用相同输入数据和相同随机种子的条件下，重构后代码的输出必须与原始代码的输出完全一致。

需要特别注意的是：如果原始代码中不存在随机过程，则不需要设置随机种子；如果原始代码中存在随机过程，但没有显式设置随机种子，则需要将相关原始代码复制到 src/LoPS/temp 中，在不改变其功能逻辑的前提下加入全局统一随机种子。随后分别运行临时旧版本代码和重构后的新版本代码，对比二者输出。如果输出完全一致，则删除 src/LoPS/temp 中用于验证的临时代码。




此外在真正执行重构任务前，我必须强调这次重构不仅仅是把代码搬运过来，而是进行一次彻底的架构模块重设计、接口重设计、代码重实现，实现真正的高内聚低耦合。恪守 KISS 原则。优先选择最直接、最清晰、最易维护的实现；避免过度工程化、过早抽象和不必要的防御性设计。



现在我们进行第一项重构，信息是：
1.脚本目录是/home/zzh/project/Pacman/2.Pac-man/structre-learning/scripts/fmriDataProcess/generateGrammar.py
2.使用conda中的fmri环境执行
3.激活conda fmri然后python /home/zzh/project/Pacman/2.Pac-man/structre-learning/scripts/fmriDataProcess/generateGrammar.py即可执行
4.数据从脚本中读取（只关注main("ghost2", 0.5, False)下的数据）
当前重构只考虑默认允许下所使用到的分支，所有没用到的分支都不考虑，此外除了当前目录你不能写其它目录更不能修改原本脚本和原本脚本所在文件夹中的内容。我删除了之前生成的run中的信息，你需要重新写这部分。



我现在需要你修改计划，每个phase我们需要完成一项重构，而不是多个phase完成一项重构。我给定你需要重构的脚本信息后，你需要首先对脚本进行深度分析，分析包括但不限于当前脚本的功能、调用了哪些模块、哪些地方使用了随机数、使用的是什么数据等。将这个分析报告写成一个文档。然后在discuss阶段你需要向我提问，当前代码中你不清楚的地方，包括但不限于功能、哪些部分直接舍弃不参与重构等。paln阶段你需要制定详细的重构计划和实施计划通过审核后进入执行阶段。最后验证。你认为是否可行（我更加关系的是重构计划可能需要经过多次讨论）


我有如下几点需要提出：
1.Cluster这个名字是怎么来的？是读取state之间的图结构吗？如果是我认为可以使用更加明确表示含义的词来表示
2.在旧版本的代码中，通过symbols来代替一个chunk比如 =代表"GL"这样的，但是我现在不像要这样的设计，不需要设计新的symbol来代替另外一个，直接使用"G-L"表示，但是这种表示方法不能基于字符串来做，所以要求输入sequence是list比较好，或者你有什么更好的想法。
3.我希望原本代码中设置的很多参数，可以暴露才类生成或者函数调用的时候，而不是写死在代码中。
4，输出结果中我希望包含两个字典，第一个字典中写所有旧的字段，第二个字典需要对旧的字段中的信息进行重新组织，删除冗余信息，然后对相关变量进行重新命名使其名字更能表达含义，希望保存的结构清晰简洁，旧版本的保存结果太过混乱




1.我觉得符号表示的方式还不够简洁，我们假设输入的是token序列，每个token是一个str比如 "G" "L" "G-L" 原始输入的就是长度为1的，学习过程中学习到更长的。
2.StrategySequenceRecord和PreparedStrategySequence是每个文件一个吗？这里面数据似乎包含了策略和状态两部分，是否有一个更加通用的名字？这里面的字段我似乎不太能理解，source_file，source_file_names是文件路径和文件代表的被试名吗？clusterFileNames中去点.pkl前面的部分其实是被试名
3

现在的问题是，所有测试当前脚本使用的数据都不要放在.planning中，而是放在data中合适的目录下，不管是需要的输入还是脚本的输出。

此外我认为当前新版本的数据输入方式并不是很对，新版本应该不依赖任何旧版本的代码和数据目录。正确的方式是将需要用到的数据迁移到当前代码库data中合适的目录中，而代码库src下的所有文件中不应该再包含非该项目的目录。
此外对于数据的输入路径通常不建议设定默认值，而是由外部脚本调用的时候传入的。用于进行比较的脚本可以在脚本中输入目标文件的路径来进行比较。


现在我需要你回答我你是否做了完整的测试，测试有哪些？

现在我需要你写入一个重要重构规则：
（1）新版本的代码必须完全不依赖旧版本的任何内容，包括但不限于旧版本代码、旧版本数据文件，所有的使用的数据必须复制到当前代码库，输出到当前代码库
（2）新版本代码中在设计阶段尽量不考虑任何对旧版本的兼容，包括对旧版本输出数据格式的兼容
（3）为了对新旧代码结果进行比对，可以专门写一个脚本或者单独的模块来进行新版本输出到旧版本输出的格式转换。


此外将当前目录文件夹名称进行一个修改，grammar2改为grammar, StateGraph改为state_graph StrategySequence改为strategy_sequence


现在我需要你加入规则：
（1）注释的意义是说明功能，解释过程，而不是与旧版本代码的比对
（2）每个函数或类必须在一开始通过""""""的方式说明改函数的功能
（3）在过程中需要在重点逻辑或者难点逻辑出写上中间注释帮助理解
加入这个规则之后对当前代码库的注释进行重写。你可以调用多个agent并行来执行


## Phase 3 算法优化讨论补充

1.GrammarLearner.learn()
（1）做ParsedSequence同意
（2）不要先做TransitionStats，当前的转移统计是基于pair_posterior，这里加了先验信息而不是纯数据统计，所以现在transitionstates然后筛选会导致无法恢复与之前完全一样的结果
2. _parse_longest() / _parse_probabilities()
    按照你的推荐来做，但是必须保证parse结果和概率统计等结果与源代码一模一样
3._organize_discrete_data() 按你的推荐改，必须保证过程更简洁高效但结果一致
4.这里我们暂时不考虑性能问题，与1一样忽略这个建议
5.detect_skip_gram() 我没有什么偏好，你可以按照你的方式改，但是必须保证功能不变最终结果不变。提示一下，映射N的方式可以改变但是映射N的逻辑，和N插入回去的位置如果改变的话那么结果很可能改变

此外我加入一点，重构之后，必须再每个函数上检查相同数据输入的时候中间处理过程中的重要指标是否在过程上一致，而不只是结果一致


## Phase 3 过程一致性验证补充

现在我希望你再做要给更加强的测试，测试目标是learn函数：
（1）输入第一个被试的数据
（2）记录每次learn迭代过程中tokens和概率，以及organized的数据

对多个时间对修改之前的脚本和修改之后的脚本进行上面三个数据的对比。如果完全一致说明过程完全一致。你可以创建临时代码和数据，运行新脚本和旧脚本保存过程信息，然后输出对比结果，如果完全一致可以删除这些临时的文件


现在使用同样的检验方法，检验当前版本的代码和旧代码库中的代码过程是否一致。这次可能涉及到新旧数据的转换以及这次在34个被试上进行检验
